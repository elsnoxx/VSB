<Window x:Class="ParkingLotWPF.Popup.ParkinglotEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editace parkoviště" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="ID:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBox Text="{Binding parkingLotId}" IsReadOnly="True" Margin="5" Grid.Row="0" Grid.Column="1"/>

            <TextBlock Text="Název:" Grid.Row="0" Grid.Column="2" VerticalAlignment="Center"/>
            <TextBox Text="{Binding name}" IsReadOnly="True" Margin="5" Grid.Row="0" Grid.Column="3"/>

            <TextBlock Text="Zeměpisná šířka:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBox Text="{Binding latitude}" IsReadOnly="True" Margin="5" Grid.Row="1" Grid.Column="1"/>

            <TextBlock Text="Zeměpisná délka:" Grid.Row="1" Grid.Column="2" VerticalAlignment="Center"/>
            <TextBox Text="{Binding longitude}" IsReadOnly="True" Margin="5" Grid.Row="1" Grid.Column="3"/>

            <TextBlock Text="Kapacita:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBox Text="{Binding capacity}" IsReadOnly="True" Margin="5" Grid.Row="2" Grid.Column="1"/>

            <TextBlock Text="Volná místa:" Grid.Row="2" Grid.Column="2" VerticalAlignment="Center"/>
            <TextBox Text="{Binding freeSpaces}" IsReadOnly="True" Margin="5" Grid.Row="2" Grid.Column="3"/>

            <Button Content="Editovat parkoviště"
                Grid.Row="3" Grid.Column="3"
                Width="140"
                Margin="0,10,0,0"
                HorizontalAlignment="Right"
                Click="EditParkingLot_Click" Height="20" VerticalAlignment="Top"/>
        </Grid>

        <DataGrid ItemsSource="{Binding parkingSpaces}" AutoGenerateColumns="False" Grid.Row="2" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID místa" Binding="{Binding parkingSpaceId}" />
                <DataGridTextColumn Header="Stav" Binding="{Binding status}" />
                <DataGridTextColumn Header="Obsazený" Binding="{Binding isOccupied}" />
                <DataGridTemplateColumn Header="Akce">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Editovat" Click="EditParkingSpace_Click" Tag="{Binding}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>