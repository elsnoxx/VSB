<Window x:Class="ParkingLotWPF.Popup.ParkingSpaceEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editace parkovacího místa" Height="250" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="ID místa:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Text="{Binding parkingSpaceId}" IsReadOnly="True" Margin="5" Grid.Row="0" Grid.Column="1"/>

        <TextBlock Text="Stav:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="StatusCombo"
                  SelectedItem="{Binding status, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  IsEnabled="{Binding CanEditStatus}"
                  Margin="5"
                  Grid.Row="1" Grid.Column="1"/>

        <Button Content="Uložit" Grid.Row="2" Grid.Column="1" Width="100" Margin="5" HorizontalAlignment="Right" Click="Save_Click"/>
    </Grid>
</Window>