import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.gpsnavigator',
  appName: 'gps-navigator',
  webDir: 'build'
};

export default config;
