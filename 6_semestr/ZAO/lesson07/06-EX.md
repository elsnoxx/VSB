# Pytorch + Klasifikace dopravních značek (dataset GTSRB)

## Popis
Cílem je navrhnout, otestovat a vzájemně porovnat dvě architektury neuronových sítí (jednu klasickou plně propojenou a jednu konvoluční) na datasetu, který je přímo součástí knihovny PyTorch (GTSRB - German Traffic Sign Recognition Benchmark).

* **Bonus 1:** Vliv barev (RGB vs. Grayscale).
* **Bonus 2:** Vizualizace predikcí.

Při tvorbě je možné využít např. následující zdroje a tutoriály:

* [Studijní materiál ZAO](https://github.com/RadovanF/CV-ML/blob/main/fundamentals/06-nn-cnn.md)
* [Training a Classifier (CIFAR-10 tutorial)](https://docs.pytorch.org/tutorials/beginner/blitz/cifar10_tutorial.html)
* [Build the Neural Network](https://docs.pytorch.org/tutorials/beginner/basics/buildmodel_tutorial.html)
* [cnn-explainer](https://poloclub.github.io/cnn-explainer/)
* [https://adamharley.com/nn_vis/](https://adamharley.com/nn_vis/)

*(Detailnější zadání najdete rozebrané níže krok za krokem.)*

---

Dataset obsahuje reálné fotografie dopravních značek rozdělených do 43 tříd (např. omezení rychlosti, dej přednost v jízdě, zákaz předjíždění atd.).

![Ukázka datasetu](imgs/dataset-01.png)

## 1. Příprava dat
K načtení datasetu použijte `torchvision.datasets.GTSRB`.
*Důležité upozornění:* Obrázky v datasetu GTSRB na rozdíl od FashionMNIST nemají jednotnou velikost a jsou barevné (3 kanály - RGB).
Než je předáte síti, musíte je sjednotit na stejnou velikost (např. 32x32 pixelů) pomocí `transforms.Resize((32, 32))`.

**Kroky:**
1. Definujte transformace (změna velikosti pomocí `transforms.Resize((32, 32))` a převod na tenzor pomocí `transforms.ToTensor()`).
2. Načtěte trénovací (`split='train'`) a testovací (`split='test'`) data.
3. Vytvořte pro ně `DataLoader` se zvolenou velikostí dávky (batch size), například 16 (můžete zkusit také 32/64).
*(Tip pro pomalejší PC: Pokud trénování trvá dlouho, můžete dataset zmenšit na 5000 náhodných obrázků. Nejprve si vygenerujte indexy `indices = torch.randperm(len(train_data))[:5000]` a použijte `Subset` z `torch.utils.data`: `train_data = Subset(train_data, indices)`)*

## 2. Návrh neuronových sítí
Vytvořte dvě různé třídy dědící z `nn.Module`. Cílem je porovnat tyto dvě architektury na stejných datech (můžete vyzoušet několik různých variant/konfigurací klasických i konvolučních sítí).

**Část A: Klasická (plně propojená) neuronová síť (MLP)**
1. Vstupní obrázek (3 barevné kanály, 32x32 pixelů) rozbalte na 1D vektor pomocí `nn.Flatten`. Vektor bude mít délku 3 * 32 * 32 = 3072.
2. Přidejte 1 až 2 skryté plně propojené vrstvy (`nn.Linear`) oddělené aktivační funkcí (`nn.ReLU`). (např. velikosti 512, 128).
3. Výstupní lineární vrstva (`nn.Linear`) se **43 výstupy** (číslo 43 reprezentuje počet tříd v našem datasetu) (bez softmax, jelikož použijete `CrossEntropyLoss`).

**Část B: Konvoluční neuronová síť (CNN)**
1. Může obsahovat několik konvolučních bloků. Blok se skládá z konvoluční vrstvy + aktivace + pooling (např. `nn.Conv2d` se 16 výstupními kanály a kernelem 3) + (`nn.ReLU`) + (`nn.MaxPool2d` s kernelem 2).
2. Přidejte druhý konvoluční blok (Conv2d -> ReLU -> MaxPool2d) .
3. Zarovnejte tenzor na 1D vektor (`nn.Flatten`).
*Tip: Pokud v konvolučních vrstvách (`Conv2d`) použijete `kernel_size=3` společně s `padding=1`, výstupní rozměry příznakových map zůstanou stejné jako původní vstup. K redukci prostorových rozměrů pak dochází výhradně ve vrstvách `MaxPool2d` (při `kernel_size=2` se rozměry zmenší na polovinu). Po dvou takových konvolučních blocích klesne rozměr z 32x32 na 16x16 a poté na 8x8. Pokud ve druhé konvoluční vrstvě zvolíte 32 filtrů, počet parametrů pro navazující vrstvu `Linear` pak bude `32 kanálů * 8 * 8 = 2048`.*
4. Přidejte plně propojenou skrytou vrstvu a nakonec výstupní vrstvu (`nn.Linear`) se **43 výstupy** (číslo 43 reprezentuje počet tříd v našem datasetu) (bez softmax, jelikož použijete `CrossEntropyLoss`).

## 3. Trénování a evaluace
Chcete-li předejít duplicitnímu kódu pro každou síť, můžete si napsat pomocnou funkci pro trénování a evaluaci: `def train_and_evaluate(model, train_loader, test_loader, epochs=5):`

1. Inicializujte oba modely a přesuňte je na grafickou kartu (GPU, pokud máte k dispozici).
2. Zvolte ztrátovou funkci (`nn.CrossEntropyLoss`) a optimalizátor (např. `optim.SGD` s parametry `lr=0.01` a `momentum=0.9` - můžete s parametry experimentovat).
3. Natrénujte obě sítě po stejný počet epoch (např. 5 až 10 epoch).
4. Vyhodnoťte úspěšnost obou modelů na testovací sadě. Před testováním úspěšnosti vždy přepněte model do validačního režimu pomocí model.eval().

## 4. Analýza výsledků
Po dokončení porovnejte výsledky klasické sítě a konvoluční sítě (můžete vyzoušet několik různých variant/konfigurací klasických i konvolučních sítí):
* Rychlost (čas potřebný k trénování po stejný počet epoch).
* Zrychlení konvergence (hodnota loss funkce na konci trénování).
* **Finální procentuální úspěšnost na testovacích datech** – o kolik je CNN lepší? Proč myslíte, že k tomu došlo?

---

## BONUS - 1: Vliv barvy na úspěšnost (RGB vs. Grayscale)
Jak moc se sítě spoléhají při rozpoznávání značek na barvu?
1. V části **Příprava dat** si vytvořte novou transformaci, která kromě `Resize` obsahuje i `transforms.Grayscale(num_output_channels=1)`. Tím odstraníte z obrázků barvu.
2. Načtěte si dataset znovu a vytvořte si pro tyto černobílé obrázky nové `DataLoadery`.
3. Upravte obě své sítě (MLP i CNN) (in_channels=1 vs. in_channels=3)
    * U MLP se změní první vrstva na `nn.Linear(in_channels * 32 * 32, ...)`.
    * U CNN se u první konvoluce změní parametr z `in_channels=3` na `in_channels=1`.
4. Spusťte trénink MLP i CNN znovu, tentokrát nad černobílými daty a porovnejte, o kolik klesla přesnost oproti barevné verzi.

## BONUS - 2: Vizualizace predikcí
Ověřte si, že se síť opravdu naučila poznávat značky tím, že si vykreslíte několik testovacích obrázků i s jejich predikovanou a skutečnou třídou. Zkuste si vytáhnout jeden batch dat (např. pomocí `iter(test_loader)`) a zobrazit obrázky pomocí `matplotlib.pyplot.imshow()`. Inspirovat se můžete přímo v oficiálních PyTorch tutoriálech.

*Tip: Aby měly grafy smysluplné popisky (např. „Stop“ místo pouhého čísla 14), zkopírujte si do kódu tento slovník:*
<details>
<summary><b>Rozbalit slovník s názvy tříd (class_names)</b></summary>

```python
class_names = {
    0: "Speed limit (20km/h)",
    1: "Speed limit (30km/h)",
    2: "Speed limit (50km/h)",
    3: "Speed limit (60km/h)",
    4: "Speed limit (70km/h)",
    5: "Speed limit (80km/h)",
    6: "End of speed limit (80km/h)",
    7: "Speed limit (100km/h)",
    8: "Speed limit (120km/h)",
    9: "No passing",
    10: "No passing veh over 3.5 tons",
    11: "Right-of-way at intersection",
    12: "Priority road",
    13: "Yield",
    14: "Stop",
    15: "No vehicles",
    16: "Veh > 3.5 tons prohibited",
    17: "No entry",
    18: "General caution",
    19: "Dangerous curve left",
    20: "Dangerous curve right",
    21: "Double curve",
    22: "Bumpy road",
    23: "Slippery road",
    24: "Road narrows on the right",
    25: "Road work",
    26: "Traffic signals",
    27: "Pedestrians",
    28: "Children crossing",
    29: "Bicycles crossing",
    30: "Beware of ice/snow",
    31: "Wild animals crossing",
    32: "End speed + passing limits",
    33: "Turn right ahead",
    34: "Turn left ahead",
    35: "Ahead only",
    36: "Go straight or right",
    37: "Go straight or left",
    38: "Keep right",
    39: "Keep left",
    40: "Roundabout mandatory",
    41: "End of no passing",
    42: "End no passing veh > 3.5 tons"
}
```
</details>

![Ukázka výstupu](imgs/dataset-02.png)

---
**Užitečné zdroje a tutoriály (PyTorch):**
* [Studijní materiál ZAO](https://github.com/RadovanF/CV-ML/blob/main/fundamentals/06-nn-cnn.md)
* [Training a Classifier (CIFAR-10 tutorial)](https://docs.pytorch.org/tutorials/beginner/blitz/cifar10_tutorial.html)
* [Build the Neural Network](https://docs.pytorch.org/tutorials/beginner/basics/buildmodel_tutorial.html)
* [cnn-explainer](https://poloclub.github.io/cnn-explainer/)
* [https://adamharley.com/nn_vis/](https://adamharley.com/nn_vis/)
